-- Linting and formatting plugins
return {
  -- ALE (Asynchronous Lint Engine)
  {
    'dense-analysis/ale',
    config = function()
      -- ALE configuration can go here
      -- For now keeping it minimal, you can add more settings as needed
    end,
  },
}
